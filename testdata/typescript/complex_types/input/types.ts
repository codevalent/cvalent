type Config = {
  host: string;
  port: number;
};

export function createServer(config: Config, options: Record<string, any>): void {
}

export function getData(): Promise<Map<string, number[]>> {
  return {} as any;
}
